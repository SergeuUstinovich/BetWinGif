import { Button } from '../../ui/Button'
import { Select } from '../../ui/Select/Select'

export const Topbar = () => {
  return (
    <div>
      <Select />
      <Button>Generare Now</Button>
    </div>
  )
}
